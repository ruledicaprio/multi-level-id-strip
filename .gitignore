# Rust
target/

# Python
.venv/
__pycache__/

# Generated gRPC stubs (produced by python/generate_grpc.py / Docker build)
python/inferer/inferer_pb2.py
python/inferer/inferer_pb2_grpc.py
python/inferer/inferer_pb2.pyi

# Local LLM weights (~1 GB — download per README)
*.gguf

# Web app upload/working directory (may contain uploaded PII)
work/

# v0.3.0 work-in-progress drafts (not yet wired into the build)
drafts/

# wasm-pack build output (built in CI for GitHub Pages)
crates/mrz-wasm/pkg/

